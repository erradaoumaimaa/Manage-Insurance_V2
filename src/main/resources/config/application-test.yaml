# application-test.yaml
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/manage_insurance
    username: postgres
    password: admin

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  swagger:
    enabled: false

  logging:
    level:
      root: ERROR